<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Eva’s Art & Poetry Journal</title>
<meta name="description" content="A tiny gallery of art, poems, photos, and zines by Eva & friends." />
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">
<style>
  :root{--bg:#faf9f7;--card:#fff;--ink:#1a1a1a;--muted:#6b6b6b;--pill:#efefef;--accent:#a78bfa;--max:1200px}
  *{box-sizing:border-box} html,body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,system-ui,Arial,sans-serif}
  a{color:inherit} header{position:sticky;top:0;background:rgba(250,249,247,.8);backdrop-filter:blur(10px);border-bottom:1px solid #eee;z-index:10}
  .nav{max-width:var(--max);margin:0 auto;display:flex;align-items:center;justify-content:space-between;padding:.9rem 1rem}
  .brand{font-weight:800;letter-spacing:.2px}.brand span{background:var(--accent);color:#fff;border-radius:10px;padding:.15rem .4rem;margin-left:.35rem}
  .actions{display:flex;gap:.5rem;flex-wrap:wrap}
  .pill{background:var(--pill);border-radius:999px;padding:.45rem .8rem;font-size:.92rem;border:1px solid #e8e8e8;cursor:pointer}
  .pill.active{background:var(--accent);color:#fff;border-color:transparent}
  .wrap{max-width:var(--max);margin:1.2rem auto;padding:0 1rem}
  .intro{display:flex;align-items:center;gap:1rem;flex-wrap:wrap}
  .intro p{margin:.2rem 0;color:var(--muted)}
  .grid{column-count:1;column-gap:1rem;margin-top:1rem}
  @media(min-width:640px){ .grid{column-count:2} }
  @media(min-width:980px){ .grid{column-count:3} }
  .card{break-inside:avoid;border-radius:18px;border:1px solid #eee;background:var(--card);box-shadow:0 8px 24px rgba(0,0,0,.04);margin:0 0 1rem;overflow:hidden}
  .card img{width:100%;display:block}
  .pad{padding:1rem}.title{font-weight:700}.by{color:var(--muted);font-size:.92rem}
  .tags{display:flex;gap:.4rem;flex-wrap:wrap;margin-top:.6rem}
  .tag{font-size:.8rem;background:#f3f3f3;border:1px solid #eee;border-radius:999px;padding:.2rem .55rem}
  .poem{font-family:"Playfair Display",Georgia,serif;font-size:1.05rem;line-height:1.7;white-space:pre-wrap}
  .btn{display:inline-block;margin-top:.6rem;border:1px solid #e7e7e7;border-radius:12px;padding:.45rem .7rem;background:#fff}
  footer{color:var(--muted);text-align:center;padding:2rem 1rem}
  .modal{position:fixed;inset:0;display:none;align-items:center;justify-content:center;background:rgba(0,0,0,.6);z-index:50}
  .modal.open{display:flex}
  .modal-box{max-width:min(900px,90vw);max-height:85vh;overflow:auto;background:#fff;border-radius:20px;box-shadow:0 20px 60px rgba(0,0,0,.25)}
  .modal-head{display:flex;justify-content:space-between;align-items:center;padding:.7rem 1rem;border-bottom:1px solid #eee}
  .close{border:none;background:#f3f3f3;border-radius:10px;padding:.35rem .6rem;cursor:pointer}
  .modal-body img{width:100%;height:auto;display:block}
  .modal-body{padding:1rem}
</style>
</head>
<body>
<header>
  <div class="nav">
    <div class="brand">Eva’s Journal <span>CAS</span></div>
    <div class="actions" id="filters">
      <button class="pill active" data-filter="all">All</button>
      <button class="pill" data-filter="art">Art</button>
      <button class="pill" data-filter="poem">Poems</button>
